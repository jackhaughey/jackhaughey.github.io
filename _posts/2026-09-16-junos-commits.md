# Junos 

Before any configurations can be applied, a root password must be set;
```
set system root-authentication plain-text-password
```
This will prompt for a password, which must be entered twice.
